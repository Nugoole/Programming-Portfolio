namespace ALT.CVL.Log.Enum
{
    public enum MainLogs { System, File, Communication, Result}
    public enum LogLevel { Fatal, Error, Warn, Info, Debug }
    public enum KeyLogger { System, Communication, File, Result }
    public enum MessengerKey { LoadConfigParams, AddJob, JobSave, JobLoad, JobDelete }
}
